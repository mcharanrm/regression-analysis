// A poor attempt to create a Jenkinsfile
pipeline {
    agent {
        label 'built-in'
    }
    stages {
        stage('testing') {
            steps {
                // Use one of the pipeline basic steps to get started
                echo "Hello World !"
            }
        }
    }
}
